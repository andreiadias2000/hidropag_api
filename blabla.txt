senacrshidropag
# Adiciona todas as modificações
git add .
# git commit -m " eewfsff "
#Sincronizar com a versão da colega
git pull upstream main
#Enviar para o repositório dela
git push upstream main


baixar 
git checkout main
git pull upstream main

branch ivan


comando para instalar todas as dependencias 

npm install @nestjs/typeorm typeorm pg @nestjs/jwt @nestjs/passport passport passport-jwt bcrypt @nestjs/config jsonwebtoken multer dotenv reflect-metadata && npm install -D @types/multer @types/jsonwebtoken @types/passport-jwt @types/bcrypt

npm install --save @nestjs/swagger

senha hash
npm install bcrypt
npm install -D @types/bcrypt

arrumar usurios login

add hash e email unico.
blablabla
vamos ver se vai agora.
funcionou